services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TRANSPORT=sse
      - REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
